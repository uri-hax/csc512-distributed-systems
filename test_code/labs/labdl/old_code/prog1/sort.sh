#!/usr/bin/env bash

# This script sorts input text in ascending order
# It reads from stdin and outputs the sorted result to stdout

sort  # Uses Bash's built-in `sort` command to arrange input lines alphabetically
